{
  "name": "@code3/spec-mcp-client-local",
  "version": "0.1.0",
  "type": "module",
  "description": "Code3 Spec-MCP Client-Local: Local spec/plan/tasks generation tools",
  "main": "dist/index.js",
  "bin": {
    "spec-mcp-local": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest",
    "test:coverage": "vitest --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}