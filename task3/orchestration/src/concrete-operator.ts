/**
 * ConcreteTask3Operator: Concrete implementation of Task3Operator
 *
 * This is a simple concrete class that extends Task3Operator.
 * It doesn't add any custom logic, just makes the abstract class instantiable.
 */

import { Task3Operator } from './task3-operator.js';

export class ConcreteTask3Operator extends Task3Operator {
  // No additional implementation needed - all logic is in the abstract class
}
